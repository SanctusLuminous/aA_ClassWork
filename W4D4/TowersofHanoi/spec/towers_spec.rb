require 'rspec'
require 'towers'

describe 'TowersofHanoi' do 

    describe "initialize" do 
        it "initializes an array of stacks and pieces"
    end

    #we need a check that we are moving a smaller piece on top of the bigger piece
    # should get a move
    # should win if everything moved to the final stack
    # 
end